# ⚡ Быстрый старт для новичков

## Что это?
Это простой фреймворк для тестирования REST API. Даже если ты никогда не писал тесты - поймешь за 5 минут! 🚀

## Запуск тестов

### Windows
Просто **дважды кликни** на файл `run-tests.bat` ✨

### Или через терминал
```bash
mvn test
```

## Как написать свой первый тест?

### Шаг 1: Создай класс
```java
public class MyTests extends BaseTest {
    // Твои тесты будут здесь
}
```

### Шаг 2: Напиши тест
```java
@Test
public void myFirstTest() {
    // Отправить GET запрос и проверить ответ
    checkStatus(get("/api/balance/all"), 200);
}
```

**Готово!** Ты написал первый тест! 🎉

## Доступные команды

### 🌐 HTTP запросы
```java
get("/api/path")              // GET запрос
post("/api/path", json)       // POST с JSON
put("/api/path", json)        // PUT с JSON
delete("/api/path")           // DELETE запрос
```

### ✅ Проверки
```java
checkStatus(response, 200)              // Проверить статус = 200
checkStatusOneOf(response, 200, 201)    // Проверить статус один из списка
```

### 📦 Получение данных
```java
text(response, "content.name")     // Получить текст из JSON
number(response, "content.id")     // Получить число из JSON
firstId("/api/balance/all")        // Получить первый ID из списка
```

### 🛠️ Создание данных
```java
newPhone()                 // Создать уникальный номер: 996801234567
json(phone, planId, userId) // Создать JSON для профиля
json(phone)                // Создать JSON с дефолтными значениями
```

## Примеры

### Простой GET тест
```java
@Test
public void getAll() {
    // Получить все балансы
    checkStatus(get("/api/balance/all"), 200);
}
```

### POST с созданием данных
```java
@Test
public void create() {
    // Создать профиль с новым номером
    String phone = newPhone();
    Response response = post("/api/admin/profile/create", json(phone));
    checkStatus(response, 201);
}
```

### Цепочка запросов
```java
@Test
public void updateProfile() {
    // 1. Получить профиль
    Response response = get("/api/admin/profile/123");
    
    // 2. Взять номер из ответа
    String phone = text(response, "content.msisdn");
    
    // 3. Обновить профиль
    checkStatus(put("/api/admin/profile/update/123", json(phone, 1, 1)), 200);
}
```

## Настройки

Файл `src/test/java/qa/config/Config.java`:
```java
BASE_URL = "http://your-server.com"  // Адрес API
ADMIN_USERNAME = "admin"              // Логин
ADMIN_PASSWORD = "password"           // Пароль
```

## Что дальше?

1. Посмотри примеры в `src/test/java/qa/tests/`
2. Скопируй любой тест и измени под себя
3. Запусти `mvn test` и смотри результат

## Полезные команды Maven

```bash
mvn test                          # Все тесты
mvn test -Dtest=BalanceTests      # Один класс
mvn test -Dtest=BalanceTests#getAll  # Один тест
mvn clean test                    # Очистить и запустить
```

## Структура проекта
```
📁 billing/
├── 📁 src/test/java/qa/
│   ├── 📁 config/          ← Настройки (URL, логин/пароль)
│   ├── 📁 helpers/         ← Авторизация
│   └── 📁 tests/           ← Твои тесты здесь!
│       ├── BaseTest.java   ← Все методы для тестов
│       ├── BalanceTests.java
│       ├── CounterTests.java
│       └── ProfileTests.java
├── 📄 run-tests.bat        ← Двойной клик = запуск
└── 📄 README.md            ← Полная документация
```

## Помощь

- Открой `README.md` для подробной документации
- Смотри примеры в `src/test/java/qa/tests/`
- Методы описаны в `BaseTest.java` с комментариями

**Удачи! 🚀**
